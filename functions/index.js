const getLocations = require('./getLocations');
const postLocation = require('./postLocation');
const sunsetLights = require('./sunsetLights');
const deployCocolist = require('./deployCocolist');

module.exports = {
  deployCocolist,
  getLocations,
  postLocation,
  sunsetLights,
};
